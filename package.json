{
  "name": "jbird",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "lint": "eslint .",
    "build": "bun run --filter '*' build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.18.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.20.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
